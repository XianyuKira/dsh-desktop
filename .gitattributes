# 仓库内统一用 LF 存储，检出时按平台转换。
* text=auto

# Windows 脚本用 CRLF，避免 PowerShell 5.1 解析问题
*.ps1 text eol=crlf

# C# 与工程文件保持 LF
*.cs    text eol=lf
*.csproj text eol=lf
*.json  text eol=lf
*.md    text eol=lf

# 二进制图标不做换行转换
*.ico binary

# 发布产物/生成物不参与 diff 统计
app/** linguist-vendored
