# 构建产物
bin/
obj/
app/
publish/
dist/

# 发布打包产物（zip 通过 git add -f 加入，或直接走 Release 附件）
artifacts/

# 由 tools/IconMaker 在构建时生成的图标
src/app.ico

# 自检与验收产物
selftest-report.txt
selftest-report.txt.error.txt
*.tail.txt
icon-preview.png

# 用户本地设置（可能包含本机路径、端口）
settings.json
webview/

# IDE / 编辑器
.vs/
.vscode/
.idea/
*.user
*.suo

# 系统垃圾
Thumbs.db
desktop.ini
.DS_Store

# 一次性准备脚本（含本机绝对路径，不公开）
prep-git.ps1

# 一次性操作脚本（含本机绝对路径，不公开）
commit-final.ps1

# 一次性修复脚本
fix-repo.ps1
# 本机调试脚本（含绝对路径，一次性用途）
launch-browser.ps1
start-browser.ps1
浏览器版 dsh web.bat
# 一次性暂存脚本
stage12.ps1
# 一次性测试/验证脚本（含本机绝对路径，不公开）
diag-*.ps1
e2e-*.ps1
final-test.ps1
iso-test.ps1
retest.ps1
visual-*.ps1
verify-zerodeploy.ps1
upload-bundle.ps1
*.e2e-backup
*.rt-backup

# 验证截图
ui-*.png
*.log
upload-zero-deploy.ps1
